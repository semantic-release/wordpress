build:
  environment:
    node: 'v18.17.0'
  nodes:
    coverage:
      tests:
        override:
          - command: 'npm run test:cov'
            coverage:
              file: coverage/clover.xml
              format: clover
